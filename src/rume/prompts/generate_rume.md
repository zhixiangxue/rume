You are generating a RUME.md file — a comprehensive skill document that
teaches both humans and future rume runs how to get this project running.

Based on the observations, execution plan, execution log, and verification
result provided below, create a detailed RUME.md document.

The RUME.md should follow this structure:

```markdown
# RUME.md — How to run {project name}

> Generated by rume — a single prompt was enough to get this running.

## Overview

Brief description of what this project does (1-2 sentences).

## Prerequisites

List everything that needs to be installed or available before running:
- Runtime (Python 3.12+, Node 20+, etc.)
- System packages (apt-get, brew, etc.)
- Environment variables or API keys needed

**If the service requires API keys or external services**, explain in
Notes what happens without them (e.g. "server starts but all API calls
return 401").

## Quick Start

The exact commands used to get the project running, step by step:
```bash
# 1. Environment isolation (ALWAYS the first step)
python -m venv .venv && source .venv/bin/activate  # or equivalent

# 2. Install dependencies
...

# 3. Configure (if any)
...

# 4. Run
...

# 5. Verify
curl http://localhost:PORT/...
```

For configuration steps, always include a minimal working snippet of
the config file content (e.g. a minimal yaml block), not just the
command to open or create the file.

If the project supports multiple install methods (e.g. source install
vs package manager install), clearly label each path and note which
one was used during this run.

## Architecture

Brief overview of the codebase structure:
- Key directories and what they contain
- Entry point
- Configuration files

## Verification

How to confirm the service is running correctly:
- Expected endpoint / health check
- Expected port
- Expected behavior

## Notes

Any important caveats, gotchas, or things to watch out for discovered
during the execution.
```

Rules:
1. Write in concise, professional English.
2. Use the exact commands that were successfully executed from the execution log.
3. Do NOT include placeholder text like "fill in X" — use real values.
4. Keep it practical — someone should be able to copy-paste the Quick Start
   and get running in minutes.
5. Output ONLY the RUME.md markdown content. No preamble, no explanation.

6. **Environment isolation is always step 1.** Include virtual environment
   creation and activation as the first Quick Start step (use the project's
   native tooling: venv/pipenv/poetry for Python, corepack for Node, etc.).

7. **Configuration steps must include a minimal working snippet.** Don't just
   write "edit config.yaml" — show the actual minimal yaml/json/toml content
   the user needs to add. Use the real values from the successful run.

8. **Avoid OS-specific commands.** Never write `nano`, `vim`, `notepad` or
   any specific editor. Always use neutral phrasing like "edit FILE with
   your preferred editor" or "add the following content to FILE".

9. **Explain failure modes for external dependencies.** If the project needs
   API keys, cloud services, or external endpoints, state clearly in Notes
   what breaks when they are unavailable (e.g. "server starts but /chat
   returns 401").

10. **Distinguish install paths.** If the project supports both source
    install (`pip install -e .`) and package install (`pip install xxx`),
    show both under clearly labeled sub-headings and specify which was
    verified during this run.
